<%= form_with(model: session) do |form| %>
  <% if session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(session.errors.count, "error") %> prohibited this session from being saved:</h2>

      <ul>
        <% session.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :subject %>
    <%= form.text_field :subject %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :grade_id %>
    <%= form.text_field :grade_id %>
  </div>

  <div class="field">
    <%= form.label :class_time %>
    <%= form.time_select :class_time %>
  </div>

  <div class="field">
    <%= form.label :class_date %>
    <%= form.date_select :class_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
